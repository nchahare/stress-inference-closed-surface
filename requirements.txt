# Python 3.11 (conda env `fem_env`). Pinned to the versions this project was developed on.
numpy==2.2.6
scipy==1.14.1
scikit-fem==12.0.1
vtk==9.6.2
matplotlib==3.10.9
meshio==5.3.5
# vedo: the project uses the git build (project_point_on_variety); PyPI >=2026.6.1 also exposes it.
vedo @ git+https://github.com/marcomusy/vedo.git
